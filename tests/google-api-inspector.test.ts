/**
 * GoogleApiInspector — source module was gutted in Phase 1.
 *
 * The implementation in src/utils/google-api-inspector.ts was removed
 * and replaced with `export {}`. These tests are retained as a
 * placeholder so the file isn't accidentally recreated with stale
 * expectations.
 */

import { describe, it, expect } from 'vitest';

describe('GoogleApiInspector (gutted)', () => {
  it('source module was deleted — no tests to run', () => {
    expect(true).toBe(true);
  });
});
