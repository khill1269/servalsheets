/**
 * Tool: sheets_format
 * Cell formatting, styles, colors, borders, conditional rules, data validation, sparklines
 *
 * Actions (25):
 * - set_format: Apply number format to cells
 * - set_background: Set cell background color
 * - set_text_format: Set text formatting (bold, italic, font, size, color)
 * - batch_format: Apply multiple formatting changes in a single operation
 * - set_borders: Set cell borders
 * - set_alignment: Set text alignment
 * - set_number_format: Apply number format pattern
 * - clear_format: Remove all formatting from a range
 * - suggest_format: AI-powered formatting suggestions
 * - auto_fit: Auto-resize rows/columns to fit content
 * - apply_preset: Apply preset styles
 * - add_conditional_format_rule: Add conditional formatting rule
 * - update_conditional_format: Update existing conditional format rule
 * - delete_conditional_format: Remove conditional format rule
 * - list_conditional_formats: Get all conditional formatting rules
 * - set_data_validation: Add data validation
 * - clear_data_validation: Remove data validation
 * - list_data_validations: Get all data validation rules (paginated)
 * - sparkline_add: Add sparkline (mini chart in a cell)
 * - sparkline_clear: Remove sparkline
 * - sparkline_get: Read sparkline configuration
 * - set_rich_text: Set rich text formatting
 * - build_dependent_dropdown: Create cascading dropdown
 */

import { z } from 'zod';

// Placeholder for actual format schema
// Full implementation in src/handlers/format.ts

export const FormatPlaceholder = z.object({});
