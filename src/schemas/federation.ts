/**
 * Tool: sheets_federation
 * Remote MCP server calls (query cross-regional or partner data)
 *
 * Actions (4):
 * - list_servers: List all connected remote MCP servers
 * - get_server_tools: Get tools available on remote server
 * - call_remote: Call tool on remote MCP server
 * - validate_connection: Validate connection to remote server
 */

import { z } from 'zod';

// Placeholder for actual federation schema
// Full implementation in src/handlers/federation.ts

export const FederationPlaceholder = z.object({});
