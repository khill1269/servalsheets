/**
 * Tool: sheets_templates
 * Reusable sheet templates (create, apply, import, list, delete)
 *
 * Actions (8):
 * - create: Save current spreadsheet as reusable template
 * - list: List saved templates
 * - get: Get template metadata
 * - preview: Preview template before applying
 * - apply: Create new spreadsheet from template
 * - update: Update template definition
 * - delete: Delete template
 * - import_builtin: Import built-in template
 */

import { z } from 'zod';

// Placeholder for actual templates schema
// Full implementation in src/handlers/templates.ts

export const TemplatesPlaceholder = z.object({});
