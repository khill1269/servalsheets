/**
 * Tool: sheets_webhook
 * Event notifications (push, subscribe, Redis-backed)
 *
 * Actions (10):
 * - watch_changes: Watch for spreadsheet changes
 * - subscribe_workspace: Subscribe to workspace events
 * - unsubscribe_workspace: Unsubscribe from events
 * - list_workspace_subscriptions: List active subscriptions
 */

import { z } from 'zod';

// Placeholder for actual webhook schema
// Full implementation in src/handlers/webhooks.ts

export const WebhookPlaceholder = z.object({});
