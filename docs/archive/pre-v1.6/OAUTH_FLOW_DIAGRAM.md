# OAuth Flow Diagram

**Visual representation of OAuth configuration loading at MCP server startup**

---

## 🔄 Server Startup Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                      MCP SERVER STARTUP                         │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 1. LOAD ENVIRONMENT                                             │
│    src/cli.ts:25                                                │
├─────────────────────────────────────────────────────────────────┤
│ dotenv.config({ path: '.env' })                                 │
│                                                                 │
│ ✅ Loaded:                                                      │
│   • OAUTH_CLIENT_ID                                             │
│   • OAUTH_CLIENT_SECRET                                         │
│   • OAUTH_REDIRECT_URI                                          │
│   • OAUTH_SCOPE_MODE (optional, default: 'full')               │
│   • ENCRYPTION_KEY                                              │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 2. CREATE GOOGLE API CLIENT                                     │
│    src/server.ts:239                                            │
├─────────────────────────────────────────────────────────────────┤
│ this.googleClient = await createGoogleApiClient({              │
│   credentials: { clientId, clientSecret, redirectUri },        │
│   tokenStorePath: '~/.servalsheets/tokens.encrypted',          │
│   tokenStoreKey: process.env.ENCRYPTION_KEY,                   │
│   // scopes: undefined (will be determined by getConfigured)   │
│ })                                                              │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 3. DETERMINE SCOPES                                             │
│    src/services/google-api.ts:182                               │
├─────────────────────────────────────────────────────────────────┤
│ this._scopes = options.scopes ?? Array.from(getConfiguredScopes())│
│                                                    │             │
│                                  ┌─────────────────┘             │
│                                  ▼                               │
│                      config/oauth-scopes.ts:88                  │
│                      getConfiguredScopes()                      │
│                                  │                               │
│             ┌────────────────────┼────────────────────┐         │
│             ▼                    ▼                    ▼         │
│      OAUTH_SCOPE_MODE     OAUTH_SCOPE_MODE     OAUTH_SCOPE_MODE│
│        = 'minimal'            = 'readonly'         = 'full'     │
│             │                    │                    │         │
│             ▼                    ▼                    ▼         │
│    MINIMAL_SCOPES         READONLY_SCOPES      FULL_ACCESS_SCOPES│
│      (2 scopes)             (2 scopes)           (8 scopes) ⭐  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 4. LOAD STORED TOKENS                                           │
│    src/services/google-api.ts:243                               │
├─────────────────────────────────────────────────────────────────┤
│ await this.loadStoredTokens()                                   │
│                                                                 │
│ Path: ~/.servalsheets/tokens.encrypted                          │
│                                                                 │
│ ┌─────────────────┐         ┌─────────────────┐               │
│ │ Tokens Found?   │         │ Tokens Missing? │               │
│ │     YES         │         │      NO         │               │
│ └────────┬────────┘         └────────┬────────┘               │
│          │                           │                         │
│          ▼                           ▼                         │
│  ┌─────────────────┐       ┌─────────────────┐               │
│  │ Decrypt Tokens  │       │ Use configured  │               │
│  │ Extract 'scope' │       │ scopes from     │               │
│  │ field           │       │ step 3          │               │
│  └────────┬────────┘       └─────────────────┘               │
│           │                                                    │
│           ▼                                                    │
│  this._scopes = storedTokens.scope.split(' ')                 │
│  ⭐ OVERRIDES configured scopes with stored scopes             │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 5. INITIALIZE GOOGLE APIs                                       │
│    src/services/google-api.ts:263-296                           │
├─────────────────────────────────────────────────────────────────┤
│ const sheetsApi = google.sheets({                              │
│   version: 'v4',                                                │
│   auth: this.auth,      // ← Has OAuth client + scopes         │
│   http2: true,          // ← HTTP/2 enabled (5-15% faster)     │
│ })                                                              │
│                                                                 │
│ const driveApi = google.drive({                                │
│   version: 'v3',                                                │
│   auth: this.auth,                                              │
│   http2: true,                                                  │
│ })                                                              │
│                                                                 │
│ const bigqueryApi = google.bigquery({                          │
│   version: 'v2',                                                │
│   auth: this.auth,                                              │
│   http2: true,                                                  │
│ })                                                              │
│                                                                 │
│ ✅ All APIs ready with active scopes                            │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 6. REGISTER MCP TOOLS                                           │
│    src/mcp/registration.ts                                      │
├─────────────────────────────────────────────────────────────────┤
│ registerServalSheetsTools(mcpServer, handlers, { googleClient })│
│                                                                 │
│ ✅ All 21 tools registered:                                     │
│   • sheets_core       (11 actions)                             │
│   • sheets_data       (15 actions)                             │
│   • sheets_format     (19 actions)                             │
│   • sheets_dimensions (27 actions)                             │
│   • sheets_visualize  (16 actions)                             │
│   • sheets_advanced   (24 actions)                             │
│   • sheets_analyze    (15 actions)                             │
│   • sheets_collaborate (33 actions)                            │
│   • sheets_composite  (13 actions)                             │
│   • sheets_bigquery   (17 actions) ⭐ Requires BigQuery scopes  │
│   • sheets_appsscript (14 actions) ⭐ Requires Apps Script scopes│
│   • sheets_webhook    (6 actions)                              │
│   • sheets_templates  (7 actions)                              │
│   • sheets_auth       (4 actions)                              │
│   • sheets_confirm    (5 actions)                              │
│   • sheets_session    (24 actions)                             │
│   • sheets_transaction (6 actions)                             │
│   • sheets_history    (7 actions)                              │
│   • sheets_quality    (4 actions)                              │
│   • sheets_dependencies (7 actions)                            │
│   • sheets_fix        (1 action)                               │
│                                                                 │
│ Total: 293 actions across 21 tools                             │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 7. SERVER READY                                                 │
├─────────────────────────────────────────────────────────────────┤
│ ✅ MCP server listening                                         │
│ ✅ OAuth configured                                             │
│ ✅ Scopes active                                                │
│ ✅ All tools available                                          │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📊 Scope Resolution Decision Tree

```
                    START: Determine Scopes
                              │
                              ▼
                 ┌────────────────────────┐
                 │ options.scopes         │
                 │ provided?              │
                 └────────┬───────────────┘
                          │
              ┌───────────┴───────────┐
              │                       │
             YES                     NO
              │                       │
              ▼                       ▼
    ┌──────────────────┐    ┌──────────────────┐
    │ Use provided     │    │ Call             │
    │ scopes exactly   │    │ getConfigured    │
    │                  │    │ Scopes()         │
    └──────────────────┘    └────────┬─────────┘
              │                       │
              │                       ▼
              │            ┌──────────────────┐
              │            │ Read             │
              │            │ OAUTH_SCOPE_MODE │
              │            │ from .env        │
              │            └────────┬─────────┘
              │                     │
              │         ┌───────────┼───────────┐
              │         │           │           │
              │    'minimal'    'readonly'   'full'
              │         │           │        (default)
              │         ▼           ▼           ▼
              │  ┌──────────┐ ┌──────────┐ ┌──────────┐
              │  │2 scopes  │ │2 scopes  │ │8 scopes  │
              │  │Basic ops │ │Read-only │ │All feats │
              │  └──────────┘ └──────────┘ └──────────┘
              │         │           │           │
              └─────────┴───────────┴───────────┘
                              │
                              ▼
                    ┌──────────────────┐
                    │ Scopes determined│
                    │ Store in         │
                    │ this._scopes     │
                    └────────┬─────────┘
                             │
                             ▼
              ┌──────────────────────────┐
              │ Check for stored tokens  │
              │ in ~/.servalsheets/      │
              │ tokens.encrypted         │
              └────────┬─────────────────┘
                       │
           ┌───────────┴───────────┐
           │                       │
    Tokens Found              No Tokens
           │                       │
           ▼                       ▼
  ┌─────────────────┐    ┌─────────────────┐
  │ Extract 'scope' │    │ Keep configured │
  │ field from      │    │ scopes from     │
  │ stored tokens   │    │ above           │
  └────────┬────────┘    └─────────────────┘
           │                       │
           ▼                       │
  ┌─────────────────┐             │
  │ OVERRIDE        │             │
  │ this._scopes    │             │
  │ with stored     │             │
  │ scopes          │             │
  └────────┬────────┘             │
           │                       │
           └───────────┬───────────┘
                       │
                       ▼
              ┌──────────────────┐
              │ FINAL SCOPES     │
              │ Active in        │
              │ Google API client│
              └──────────────────┘
```

---

## 🔐 Token Storage Structure

```
~/.servalsheets/tokens.encrypted
         │
         ├─ Encrypted with AES-256-GCM
         ├─ Key: ENCRYPTION_KEY from .env
         │
         └─ Decrypted structure:
            {
              "access_token": "ya29.a0...",     // Short-lived (1 hour)
              "refresh_token": "1//0g...",      // Long-lived (months)
              "expiry_date": 1706907600000,     // Unix timestamp
              "token_type": "Bearer",
              "scope": "https://...spreadsheets https://...drive ...",  ⭐
              "id_token": "eyJhbG..."           // JWT with user info
            }
                              │
                              │ scope field extracted
                              ▼
              Split by space: ['https://...spreadsheets', 'https://...drive', ...]
                              │
                              │ Becomes
                              ▼
              this._scopes = [...] (overrides configured scopes)
```

---

## 🌐 HTTP Mode Differences

In HTTP mode, there's an additional OAuth provider flow:

```
┌─────────────────────────────────────────────────────────────────┐
│ HTTP SERVER STARTUP (src/http-server.ts)                        │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ If enableOAuth: true                                            │
│   Create OAuthProvider                                          │
│   └─> Exposes /oauth/authorize endpoint                        │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ User visits /oauth/authorize                                    │
│   ↓                                                             │
│ Redirect to Google OAuth                                        │
│   ↓                                                             │
│ Include scopes from:                                            │
│   formatScopesForAuth(getRecommendedScopes())                   │
│   ↓                                                             │
│ Returns: ALL 8 scopes in authorization URL                      │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ User grants permissions on Google consent screen                │
│   ↓                                                             │
│ Google redirects back with authorization code                   │
│   ↓                                                             │
│ Exchange code for tokens                                        │
│   ↓                                                             │
│ Tokens include 'scope' field with granted scopes                │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ Create session-specific GoogleApiClient                         │
│   └─> Uses tokens from OAuth flow                              │
│   └─> Scopes come from tokens.scope                            │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🎯 Key Insights

### 1. **Configuration Priority**

```
Explicit Options > Stored Tokens > Environment Config > Default
      (code)          (file)            (.env)          (full)
```

### 2. **Default Behavior**

- If nothing configured: **8 comprehensive scopes**
- After `npm run auth`: **Scopes from authentication**
- With `OAUTH_SCOPE_MODE=minimal`: **2 basic scopes**

### 3. **Stored Tokens Win**

If `~/.servalsheets/tokens.encrypted` exists:

- Extract `scope` field from tokens
- **Override** configured scopes
- Use stored scopes for all operations

### 4. **Single Source of Truth**

All scope configurations flow through:

```
src/config/oauth-scopes.ts
  ├─> FULL_ACCESS_SCOPES (8 scopes)
  ├─> MINIMAL_SCOPES (2 scopes)
  └─> READONLY_SCOPES (2 scopes)
```

### 5. **Consistent Everywhere**

- STDIO mode: Uses same scope config
- HTTP mode: Uses same scope config
- OAuth provider: Uses same scope config
- CLI auth: Uses same scope config

---

## 🔍 Runtime Verification

### Check What's Loaded

**In Code**:

```typescript
const scopes = this.googleClient.scopes;
console.log('Active scopes:', scopes);
```

**Via HTTP**:

```bash
curl http://localhost:3000/health | jq '.scopes | length'
# Should output: 8 (for full mode)
```

**In Logs**:

```bash
npm run start:http
# Look for: "Google API clients initialized"
# Should show: "All APIs ready with 8 scopes"
```

---

**Last Updated**: 2026-02-03
**Version**: 1.6.0
**Status**: ✅ Verified
